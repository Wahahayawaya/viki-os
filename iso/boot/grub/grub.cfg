# GRUB配置文件 - 用于引导VIKI操作系统

# 设置默认启动项
set default=0

# 设置超时时间（秒）
set timeout=1

# 定义启动菜单项
menuentry "VIKI OS" {
    # 使用multiboot2命令加载符合multiboot2协议的内核
    multiboot2 /boot/kernel.bin
    # 启动内核
    boot
}
